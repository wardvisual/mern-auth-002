import http from "../services/httpService";
import {
  LIST_PRODUCT_REQUEST,
  LIST_PRODUCT_SUCCESS,
  LIST_PRODUCT_FAIL,
} from "../consonants/products";

export const listProducts = () => (dispatch) => {};
